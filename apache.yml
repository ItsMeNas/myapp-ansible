---
- hosts: webservers 
  become: True
  become_user: nshariff
  tasks:
    - name: Install Apache
      apt:
        name: apache2
        state: latest
    - name: Start Apache server
      service:
        name: apache2
        state: started
        enabled: True
    - name: Deploy static website
      copy:
        src: index.html
        dest: /var/www/html/
      notify:
        - restart httd
